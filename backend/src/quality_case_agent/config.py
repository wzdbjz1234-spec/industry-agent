"""Application configuration boundary.

Concrete settings are introduced with the runtime bootstrap task. Keeping this module at the
package root makes configuration a shared entrypoint concern rather than a domain dependency.
"""
