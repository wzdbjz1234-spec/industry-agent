# Upstream data-loss scenario

Phase 19 replay fixture: emit sparse windows, mixed detector versions, or late
records. The monitoring policy must return `DATA_QUALITY_BLOCK` and prevent a
process Case from being opened.
