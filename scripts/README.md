# Repository scripts

Scripts for schema generation, OpenAPI generation, demo seeding and contract verification will be
added as their corresponding implementation tasks land. `verify_package_structure.py` is the
initial structure smoke check.
